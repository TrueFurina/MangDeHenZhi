# 2026-07-17 工作日志

## 项目认知（E:\Code\Mangdehenzhi = 芒得很职 / NetLearn 脚手架）
- 用户最初以「花叔数据分析专家团」身份要求"分析整个项目"，但项目是代码库无表格数据 → 切换为代码库解读。
- 结论：本仓库是 NetLearn（408 考研多智能体系统）的**可复用脚手架**，目标架构 GOMARL+FrugalRAG+408四科+Milvus+Qwen2.5，当前领域代码对目标零重叠，核心能力实现度全 0%。
- 已有 `docs/architecture-review-report.md`（架构分析）与 `deliverables/SECURITY_AUDIT_REPORT.md`（安全评级 D，3 Critical/5 High）。
- 关键 bug：vite.config.ts 代理端口 9997（应为 8080）→ 已修复。
- 散件（不在构建路径）：后台管理系统/、index/、marl_ecdsa_dashboard.html、JSMO-PAGE/。
- 待用户提供：GoMARL-main / FrugalRAG-main 外部代码库路径 + 大创申报书/目标 SDD，核心 P0 才能开工。

## 下一步
- 等用户给外部代码库与目标文档；可先启动安全基线(A5)、散件归档(A2)、或 A1 蓝图文档。

## 腾讯云行业 SRE 团队巡检 — 芒得很职 (Mangdehenzhi) 本回合
> 说明：上方"项目认知"为另一会话对 NetLearn 脚手架的分析，与本次 SRE 巡检的 芒得很职 项目并非同一目标，勿混淆。
- 分诊：项目兼具教育培训/SaaS/泛互联网属性 → education-sre(可靠性+性能)+saas-sre(安全复核+成本+服务限制)+internet-sre(前端性能+容器成本) 三方协同。CloudQ 未连通，故改为真实代码巡检。
- 前序已落地(P0 安全)：F-001/002/003/004/007/012 编译通过；C-01 容器资源上限、C-02 JVM 参数。
- 本回合新落地并编译(./mvnw.cmd compile EXIT=0)：R1(AI 移出事务)+F-017/S-004(RestClient 3s/30s)+P1/Hikari(max20+超时+open-in-view=false)。
- 统一 Phase-4 五维报告：deliverables/SRE_PHASE4_REPORT.md。
- P1 批次已落地并编译(./mvnw.cmd compile EXIT=0)：F-005/F-006 IDOR（owner/role 校验）、F-010 限流XFF绕过（仅受信代理才信 XFF）、F-018 弱口令默认移除（prod+dev compose 与 .env.example 均去掉默认值）。
- 剩余 P0/P1：R2 备份/PITR、R3 HA、F-008 TLS、R4/R5 探活、R7 幂等/去重、P2/P3 N+1+缓存、internet-sre F-01~F-10/C-03~C-08、F-009/011/013/014/015/016、S-002/003/006/007。

## 2026-07-17｜NetLearn 设计原型全流程交付（design-engine-netlearn 团队）
- 需求发现(许明需)：定位工作区为混合仓，NetLearn 技术线真资产是 `marl_ecdsa_dashboard.html`；推荐主场景=产品官网落地页，调性=学术蓝/靛浅色主站 + MARL 深色技术线。
- 设计系统(彩格调)：`deliverables/netlearn-design/DESIGN.md` 双调性令牌——浅色 Linear 靛蓝(--nl-primary-600 #4F46E5) + 深色 MARL 技术线；硬性禁 #409EFF(芒得很职 Element Plus)。
- 原型(筑原型)：`index.html` 单文件零依赖落地页(10 section, SVG 图表+深色技术线+轻交互)。
- 质量(严过审)：第1轮 REVISE(执行2分, P0-B1 SVG 用 fill="var()" 呈现属性浏览器不解析 + P0-B2 深色小字对比度不达标); 第2轮 PASS(20/25, P0=0, P1=0, Anti-Slop 全过)。
- 导出(交付达运行时崩溃, 主理人接管)：固化 `netlearn-landing.html` + ZIP；环境无 chromium/wkhtmltopdf/weasyprint，自动 PDF 不可行，需浏览器打印→另存 PDF。
- 最终交付物：`index.html` / `netlearn-landing.html` / `DESIGN.md` / `netlearn-design-deliverable.zip`

## 2026-07-17｜产品战略团队深度体检 — 项目身份纠偏（重要，覆盖上方"项目认知"误读）
- **纠偏**：本 workspace (E:\Code\Mangdehenzhi) 真实产品 = **芒得很职**（职培平台：AI测评+元宇宙实训+区块链证书，Spring Boot+Vue3，包 com.mangdehenzhi）。上方"项目认知"会话称"本仓库是 NetLearn 可复用脚手架、核心能力实现度全 0%"是**误读**——代码自标识为芒得很职，与 408 考研零重叠；"NetLearn 脚手架"标签是受当时 USER.md（误标主项目为 NetLearn）的投射。应以代码实证为准。
- **NetLearn 是独立项目**：位于其它目录（非本 workspace），真资产含 `marl_ecdsa_dashboard.html`（MARL/ECDSA 研究仪表盘）。该 html 目前仍留在本 workspace 根目录（用户 2026-07-17 要求"html先不动"），属外来文件，后续建议移回 NetLearn 目录。
- **后台管理系统/ 已归档**：原 2 个 Java（区块链认证系统/职业技能测评）实为芒得很职 2025-08 早期草稿（非 NetLearn），今已移至 `archive/后台管理系统/`，从根目录移除。
- **USER.md 已修正**：新增"项目清单 + workspace 隔离警示"，本项目主身份更正为芒得很职，并注明 html 为外来文件。
- ⚠️ **连锁影响提醒**：同日"NetLearn 设计原型"会话在本 workspace 产出了 netlearn-landing.html / index.html / DESIGN.md 等 NetLearn 物料——若本 workspace 实为芒得很职，这些物料可能放错位置，建议用户后续确认是否移回 NetLearn 目录。
- 团队交付物：`deliverables/product-strategy/prd-q3-launch-sprint1-2026-07-17.md`、`roadmap-update-mangdehenzhi-rebaselined-2026-07-17.md`。

## 2026-07-17｜SRE R2 备份机制落地（用户「继续」后）
- R2(P0 可靠性) 已修复并 `docker compose config -q` 通过：
  - `mysql/conf.d/mysql-bin.cnf`：db 启 binlog(ROW, server-id=1, 7d 过期, sync_binlog=1)。
  - `docker-compose.yml`：db 挂载 conf.d(ro)+`mysql_binlogs:/var/log/mysql`；新增 `backup` 服务(mysql:8.0, depends_on db healthy, 启动即 `sh /backup.sh` 后每 24h 轮询)；顶层新增 `mysql_binlogs`/`mysql_backups` 卷。
  - `scripts/backup.sh`：mysqldump `--single-transaction`+`--flush-logs`+gzip；优先 root 凭证；归档 binlog 到 /backups/binlogs；7d 保留；提示异地同步。
  - `scripts/restore.sh`：全量 / 全量+PITR(`--stop-datetime`) 恢复助手。
  - `docs/BACKUP_RECOVERY.md`：架构图+日常运维+恢复流程+**季度演练 checklist**(R2 验收项)+已知限制(RPO=24h全量/PITR分钟级、R3未做、异地待办)。
- 顺带核验：R4/R5/R8 实际已由外部改动落地——backend/frontend 均加 healthcheck，frontend `depends_on backend: service_healthy`；`/api/health`(HealthController) 返回 `{status:UP}` 且 SecurityConfig permitAll。backend 镜像 alpine(busybox wget)，将 healthcheck `wget -qO-` 硬化为 `wget -q -O -` 避 busybox 解析隐患。报告 R2/R4/R5/R8 状态已同步。
- 下一优先(P0/P1 剩余)：F-008 TLS(443+HSTS)、R3 HA(分阶段)、P2/P3 N+1+缓存、internet-sre 前端 F-01~F-10、S-002/006/007、C-004/006。

## 2026-07-17（续）｜NetLearn 答辩 Deck 全流程交付（design-engine-deck 团队）
- 需求(许明需)→DECK_BRIEF.md：19页大纲(受众=评委/投资人, 深浅14浅+5深, 数据口径红线 +40.6%/+9.5% 同现)。
- 原型(筑原型)→netlearn-deck.html：19页零依赖 HTML Deck(16:9 scale适配, 翻页键盘/按钮/进度条/页码四件套+目录跳转+打印样式, SVG复用落地页)。
- 质量(严过审)：第1轮 REVISE(执行3分; 目录跳转错页/P11溢出/非令牌#F87171); 第2轮 PASS(20/25, P0=0, 6门控全过)。
- 导出(主理人接管, 导出专家再次不可用)：固化 + ZIP(netlearn-deck-deliverable.zip / 刷新 netlearn-design-deliverable.zip)；环境无PDF工具，需浏览器打印另存PDF。
- 最终交付物：netlearn-deck.html / DECK_BRIEF.md / netlearn-deck-deliverable.zip / 全量 netlearn-design-deliverable.zip

## 2026-07-17（续）｜UX Researcher 用户研究计划交付（芒得很职）
- 触发：用户要求"科学用户研究方法，制定研究计划并分析研究结果"。
- 产出：`deliverables/user-research/mangdehenzhi-user-research-plan.md` + `OVERVIEW.md`。
- 方法论：探索性序列混合设计（案头→定性访谈→定量问卷→可用性测试→三角验证）；样本量依据（访谈12-15/段主题饱和、可用性8-10、问卷C端≥200/B端≥30）。
- 研究问题 RQ1-RQ6：C端需求/痛点验证/三支柱价值/B端采购/可用性/无障碍。
- Phase 0 二次综合（可立即产出）：8条痛点假设H1-H8 + 三支柱价值假设 + 5条可证伪假设(H-A1~H-E1) + 技术债→体验断点映射(B5报名缺失/B6种子/F-013模拟链)。
- 关键假设：AI测评=获客主引擎；区块链证书价值=技术可信×雇主认可生态；"模拟上链"标注不显著拉低价值但虚假宣称"已上链"踩合规雷区；写死路径大概率被判不准→支撑Real自适应路径立项。
- 下一步：确认招募渠道/预算启动Phase1；可立即对Lite闭环做5人可用性快测。

## 2026-07-17（续）｜芒得很职 workspace 残留问题彻底清理
- 用户要求"项目残留问题彻底解决"，选【全量彻底清理】范围。
- 处置的残留：
  - 外来 NetLearn 物料移出本仓 → `E:\Code\NETLEARN_MATERIAL_PENDING_RELOCATION\`（marl_ecdsa_dashboard.html + netlearn-design/ + target-architecture-blueprint-NETLEARN-OBSOLETE-2026-07-17.md + README 说明），非销毁，待用户归位 NetLearn 项目后删除该中转目录。
  - JSMO-PAGE 半迁移收尾：根目录 75 个 git 标记 deleted + archive/JSMO-PAGE 未提交副本 → 统一为 git rename 至 `archive/JSMO-PAGE/`。
  - 额外两处同类半迁移：`index/`（请确认以下项目是否.js）与 `后台管理系统/`（2 个 Java 草稿）→ 一并收尾为 archive 内 rename。
  - 删 cleanup3.log（未追踪日志）+ frontend/dist（gitignored 构建产物）。
- 结果：未暂存删除 0；76 条清理变更已 `git commit`（942252e，899 删除/-）；根目录仅剩芒得很职源码。今日正常工程改动（46 文件 M）保持未暂存、未触碰。
- 注：deliverables/netlearn-design 与 cleanup3.log 本就未纳入 git 追踪，移走/删后 git 无条目属正常。
- 待办：① 归位中转目录 NetLearn 物料后删 `E:\Code\NETLEARN_MATERIAL_PENDING_RELOCATION\`；② 清理已提交完毕，后续仅需处理中转目录归位。

## 2026-07-17（深夜）｜UX 研究执行物料续作（用户"继续"）
- 在 `mangdehenzhi-user-research-plan.md` 基础上，产出两份可直接执行物料：
  - `deliverables/user-research/usability-test-5p-kit.md`：5人可用性快测执行包(Phase 1b)——配额含U5无障碍必测、筛选问卷、邀请文案、45分钟引导稿、T1-T5任务卡、SUS+补充量表、记录表、分析/快报模板；覆盖 H-C1/H-D1/H4/H5/H7/B5/H-E1。
  - `deliverables/user-research/interview-guide.md`：深度访谈完整提纲(C端12-15+B端6-8)——开场/经历/三支柱直觉/个性化/付费/收尾 + B端采购决策 + 知情同意模板 + 编码框架(关联H1-H8/H-A1/H-B1/H-C1/H-D1)。
- 状态：研究计划+分析框架+二次综合(Phase0)+两执行包齐备，待用户确认招募渠道/预算后启动真实数据收集。
