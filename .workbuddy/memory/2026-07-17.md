# 2026-07-17 工作日志

## GStack 全员深度工作（团队 gstack-deepwork-2026-07-17）
调动 5 名成员对两条战线并行作业：

### 战线 A：MARL Dashboard（E:/Program/MARL/【CCF】区块链AI协同：面向MARL的共识机制/marl-ecdsa-consensus-chain）
- 排障手修复两大问题并已验证：
  1. 前端 `toFixed` 崩溃：真凶在 `templates/dashboard.html:2192`（Canvas 权重图缺空值保护），用户所述"1582行"是旧版本行号偏移。共 4 处兜底（1463/1552/2148/2192）。
  2. 数据缺失：`scripts/generate_complete_data.py` 的 BASE_DIR 指向 `scripts/` 子目录而 Dashboard 从项目根读 → 路径错位，根目录 selfish 一直是 278 字节桩。已修正路径 + 写入前校验 + `--mode` 参数；selfish 重生为 1000 回合完整数据。
  - 澄清：pure/bc 本就各 1000 条 episode_rewards（非空），"pure/bc 仅100回合/episode_rewards 空"为旧缓存态误判；真正缺口只有 selfish。
- QA 质量门神：🟡 有条件通过。残留 P1：bc（BC-MARL）模式 `bc_scores_history` 仍为空 → monitor/consensus 两张积分图空状态（不崩溃）。selfish 已补、pure 预期无区块链。
- 设计师：视觉审查 2 个 Critical（C1 配色双轨、C2 空状态图表无占位）；产出 19 页零依赖 HTML 模板 `ppt_template/index.html` + `DESIGN_REVIEW.md`。
- 产品官：19 页 PPT 叙事大纲（7 Dashboard 整页 + 11 图全纳入）。提示 +40.6%(总奖励) vs +9.5%(公平环境) 口径需披露；generate_ppt.py 读 `results/` 路径错位，应改读仓库根。

### 战线 B：当前工作区安全审计（E:\Code\Mangdehenzhi）
- 安全卫士评级 D（高风险），26 项发现。3 个 Critical：
  - F-001 提交的 JWT 密钥（application.yml/docker-compose/.env.example 同公开串）
  - F-002 默认种子口令 admin/admin123 等
  - F-003 匿名可写课程（/api/courses/** permitAll）
  - F-004 方法级鉴权完全缺失（根因）
- 报告落盘：`E:\Code\Mangdehenzhi\deliverables\SECURITY_AUDIT_REPORT.md`

### 综合收口
- 报告：`E:\Code\Mangdehenzhi\deliverables\gstack\deepwork-all-2026-07-17.md`
- 关键行动项：P0 轮换 JWT 密钥/移除默认口令/启用方法鉴权；P1 补 bc_scores_history + noData 占位 + TLS；P2 PPT 口径披露。
- 注：仓库无 git，回滚靠文件备份（training_results_selfish_backup.json）。

### 战线 C：产品战略团队 · 竞品分析(W2) + 路线图规划(W3) — 芒德矩阵
- 主理人方向明按铁律建团队、成员独立产出、主理人中转、落盘 `deliverables/product-strategy/`。
- W2 竞品分析：`competitive-analysis-mangdehenzhi-2026-07-17.md`（竞品全景、7+1 功能矩阵、市场数据、SWOT、6 条差异化空白、8 条行动）。
- W3 路线图规划（团队 product-roadmap-mangdehenzhi）：三方素材齐（数析指标评审[data-analyst-2 有效产出，原 data-analyst 网络故障2次未参与]/瑞思用户反馈/竞析竞品动态），路径整合。
  - 核心结论：芒德是当前唯一切面覆盖 A AI测评+B 元宇宙实训+C 区块链认证 的产品；拥有 12–18 月「整合叙事」窗口。
  - 排期：Q3=P0 A深化+个性化+轻量督学+并行启动 B 立项/C 备案；Q4=P1 B 标杆+C MVP（依赖备案前置）；远期=P2 雇主生态+技能信用闭环+虚拟面试。
  - 最高前置项：C 网信办备案（Q3 即启动，硬依赖）；B 借政策窗口卡位（极高紧迫）。
  - 落盘：`roadmap-update-mangdehenzhi-2026-07-17.md`（含路线图表/高管·工程·设计三版沟通/竞争监测看板/风险登记）。
- 备注：原 `data-analyst` Agent 因 `copilot.tencent.com` DNS 解析 502 失败两次，已用 `data-analyst-2` 重试并完成有效产出；不视为成员结论缺口。

## MARL Dashboard 收尾修复（晚间，主理人直落）
agent 后端再遇 502（copilot.tencent.com DNS 失败），排障手两次拉起失败；主理人改用自有工具直接修复并验证。
- P1：磁盘 `training_results_bc.json` 是旧版残缺文件（仅4字段），当前脚本 bc 分支本就写 bc_scores_history。用项目 `.venv` 重跑 `scripts/generate_complete_data.py --mode bc` → 重生成 13 字段完整 bc 数据（bc_scores_history len=1000），旧文件备份为 `training_results_bc_backup.json`。`GET /api/compare` 验证 bc 现含该字段。
- P2：按 `DESIGN_REVIEW.md §5` 在 `templates/dashboard.html` 落地 emptyStatePlugin（Chart.js afterDraw 空状态占位）+ chart-bc/chart-bc-history 分级文案（bc缺失/未启用区块链）+ consensus/explorer 两页 `.empty-state` 容器与切换逻辑；`static/dashboard.css` 加 `.empty-state` 样式。源码 grep 命中 6 处。
- 验证：`/healthz` ok；bc bc_scores_history=1000；前端运行时未实跑（环境无无头浏览器）。
- 报告：`E:\Code\Mangdehenzhi\deliverables\gstack\dashboard-fix-2026-07-17.md`
- 注意：agent 执行后端（copilot.tencent.com）今日多次 502，子 Agent 不稳定；必要时主理人直落。
